<h3>Neighborhoods</h3>

<ul>
  <% @neighborhoods.each do |neighborhood| %>
   <li><%= link_to neighborhood.name, neighborhood_path(neighborhood) %></li>
  <% end %>
</ul>

<div class="bottom-nav">

</div>
